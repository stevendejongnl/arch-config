# Conservative charge thresholds — laptop stays plugged in most of the
# time, so cap charging to slow further battery wear (was at 87.2%
# capacity / 510 cycles when this was set, firmware default is 95/100).
START_CHARGE_THRESH_BAT0=75
STOP_CHARGE_THRESH_BAT0=80
