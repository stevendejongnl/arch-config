# Base packages installed on all systems
# These packages are included regardless of host or modules

description: Base system packages

packages:
  # Essential base system
  - base
  - base-devel

  # Kernel
  # - linux              # Standard kernel
  # - linux-zen          # Zen kernel (optimized for desktop)
  - linux-lts           # Long-term support kernel
  # - linux-hardened     # Security-focused kernel

  # Firmware
  - linux-lts-headers
  - linux-firmware
  - aic94xx-firmware    # Firmware for Adaptec/LSI SAS controllers
  - ast-firmware        # Firmware for ASPEED AST graphics

  # System
  - arandr
  - amd-ucode           # AMD CPU microcode updates
  - acpi                # ACPI support
  - auto-cpufreq        # Automatic CPU frequency scaling
  - blueman             # Bluetooth manager
  - efibootmgr          # EFI boot manager
  - lxappearance        # Appearance settings manager
  - lxsession           # LXDE session manager
  - mesa-utils          # Mesa utilities (e.g., glxinfo)
  - paccache-hook       # Tool to manage pacman cache
  - sudo
  - upd72020x-fw        # Firmware for Realtek RTL8723BU Wi-Fi/BT chipsets
  - update-grub         # Update GRUB bootloader
  - v4l2loopback-dkms   # V4L2 loopback device driver
  - wd719x-firmware     # Firmware for Marvell Yukon Ethernet controllers
  - xf86-video-vesa     # VESA video driver for Xorg

  # Window manager
  - picom               # Compositor for X11
  - rofi                # Application launcher and dmenu replacement
  - xorg-server         # Xorg display server
  - xorg-xhost          # Xorg host program
  - xorg-xinit          # Xorg initialization program
  - xorg-xinput         # Xorg input device configuration tool
  - xorg-xrandr         # Xorg screen resizing and rotation tool
  - xorg-xset           # Xorg user preferences (screensaver/DPMS off in xinitrc)
  - xorg-xsetroot       # Xorg root window parameter setting tool
  - xorg-xprop          # Xorg window property utility
  - xorg-xbacklight     # Xorg backlight control utility

  # Audio
  - pipewire            # Audio and video server
  - pipewire-alsa       # ALSA support for PipeWire
  - pipewire-audio      # Audio support for PipeWire
  - pipewire-jack       # JACK support for PipeWire
  - pipewire-pulse      # PulseAudio replacement for PipeWire
  - pipewire-zeroconf   # Zeroconf support for PipeWire

  # Basic tools
  - git
  - neovim
  - btop
  - alacritty
  - xterm               # fallback terminal (dmenu_run, scripts)
  - autorandr
  - bind                # DNS server and tools
  - bluez-qt            # Bluetooth tools for Qt-based environments
  - bluez-tools         # Bluetooth command-line tools
  - caffeine-ng         # Prevents system from sleeping
  - pavucontrol         # PulseAudio volume control
  - tmate               # Instant terminal sharing
  - tmux                # Terminal multiplexer
  - unclutter           # Hide mouse cursor when idle
  - x11-ssh-askpass     # SSH password prompt for X11
  - xaskpass            # SSH password prompt for X11
  - xdg-desktop-portal-gtk  # GTK implementation of XDG desktop portal
  - zoxide              # Fast directory jumper
  - zsh                 # Z shell
  - android-tools       # Android platform tools (adb, fastboot)
  - jdk                 # Java Development Kit
  - jdk17-openjdk       # OpenJDK 17
  - direnv              # Environment variable manager
  - wakeonlan           # Wake-on-LAN utility
  - tcpdump             # Network packet analyzer
  - lazydocker          # Simple terminal UI for Docker and Docker Compose

  # Applications
  - thunar              # File manager
  - thunar-archive-plugin  # Archive support for Thunar
  - thunar-media-tags-plugin  # Media tags support for Thunar
  - thunar-shares-plugin  # Network shares support for Thunar
  - thunar-volman       # Volume management for Thunar
  - ungoogled-chromium-bin
  - zapzap             # WhatsApp desktop client
  - slack-desktop
  - jetbrains-toolbox
  - barrier            # Keyboard and mouse sharing
  - pep                 # System tray keep-awake tool (systemd-inhibit)
  - kdeconnect          # Cross-device link (phone/desktop) over LAN
  - plasma-browser-integration  # Native host for browser "send tab" via kdeconnect

  # Password managers
  - 1password
  - 1password-cli
  - bitwarden
  - bitwarden-cli

  # Storage / removable media / boot
  - grub
  - gvfs               # Thunar trash, mounts, MTP
  - udisks2            # removable drive mounting
  - tumbler            # Thunar thumbnails
  - ntfs-3g
  - mtools
  - os-prober

  # Media / audio / fonts
  - playerctl          # tmux + statusbar "now playing"
  - ffmpeg
  - noto-fonts
  - ttf-dejavu
  - ttf-liberation

  # Bluetooth
  - bluez

  # Dev
  - go
  - clang
  - github-cli
  - nodejs-lts-jod     # required by bitwarden-cli; dev Node is managed by nvm
                       # (bootstrapped in scripts/dotfiles-deploy.sh, loaded via
                       # ~/.zsh-config/nvm.zsh / ~/.bashrc)

  # WM helpers
  - brightnessctl
  - numlockx
  - xdotool
  - flameshot
  - feh                # wallpaper.sh
  - dunst              # notification daemon (notify-send)
  - rofi-bluetooth-git
  - wireplumber        # pipewire session manager
  - openssh
  - thefuck
  - usbutils

  # dcli itself
  - dcli-arch-git

  # AI Tools
  - claude-code

  # dcli dependencies
  # - paru         # AUR helper (required for AUR package management)
  - yay          # AUR helper (required for AUR package management)
  - fzf          # Fuzzy finder (required for dcli search/module/backup TUI)
  - timeshift    # System backup tool (required for dcli backup commands)

  # Dotfiles and secrets management
  - git-crypt    # Transparent git encryption for secrets
  - stow         # Symlink farm manager for dotfiles deployment
  - rsync        # Fast incremental file transfer for backups

  # Rime theme (git.madebysteven.nl/stevendejong/rime) — installed via
  # post_install_hook below. Its Qt component needs qt6ct/qt5ct, declared
  # in modules/dark-mode.yaml (always-enabled on both hosts).

  # Icon theme matching the rime palette (vinceliuice/Colloid-icon-theme,
  # Dracula scheme, Purple accent)
  - colloid-dracula-theme-git

  # GTK widget theme (dotfiles' gtk-3.0/4.0 settings.ini sets
  # gtk-theme-name=Dracula on every host, so this needs to be everywhere too)
  - dracula-gtk-theme

conflicts: []

# Clones/updates ~/workspace/rime and runs `./rime install --all`
# (terminal, tmux, rofi, dunst, GTK, Qt, Chromium, VSCodium, dark-mode, dwm)
post_install_hook: "scripts/rime-theme.sh"
