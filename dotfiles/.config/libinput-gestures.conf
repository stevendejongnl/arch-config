# 3-finger swipe for dwm tag navigation
# Maps to shiftviewclients keybindings in dwm config.def.h
# Direction: fingers move left → next tag, fingers move right → prev tag (natural scrolling)
gesture swipe left  3  xdotool key super+bracketleft
gesture swipe right 3  xdotool key super+bracketright
