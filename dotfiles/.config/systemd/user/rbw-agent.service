[Unit]
Description=rbw agent (Bitwarden CLI unlock cache)

[Service]
Type=forking
PIDFile=%t/rbw/pidfile
ExecStart=/usr/bin/rbw-agent
Restart=on-failure
RestartSec=1

[Install]
WantedBy=default.target
